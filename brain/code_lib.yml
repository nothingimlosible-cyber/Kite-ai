- t: JavaScript Fetch
  s: |
    Untuk mengambil data dari API:
    ```javascript
    async function getData(url) {
      const response = await fetch(url);
      const data = await response.json();
      return data;
    }
    ```
  d: 2026-05-12

- t: CSS Center Div
  s: |
    Cara paling modern mengetengahkan elemen:
    ```css
    .container {
      display: grid;
      place-items: center;
      height: 100vh;
    }
    ```
  d: 2026-05-12
